class intern {
    constructor(name, id, email){
        this.name = name;
        this.id = id;
        this.title = "employee";
        this.email = email;
    };

* `getName()`

* `getId()`

* `getEmail()`

* `getRole()`&mdash;returns `'Employee'`